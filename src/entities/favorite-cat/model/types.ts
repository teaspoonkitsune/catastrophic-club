export type FavoriteCat = {
  id: string;
  imageUrl: string;
  addedAt: Date;
};

export type FavoriteCatRecord = {
  id: string;
  imageUrl: string;
  addedAt: string;
};

export type FavoriteCatInput = {
  id: string;
  imageUrl: string;
};
